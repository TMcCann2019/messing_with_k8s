apiVersion: v1
kind: Service
metadata:
  name: api-svc
  namespace: messing
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: {{.Values.service.backend.type}}